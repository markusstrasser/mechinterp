# Quick test configuration for debugging

p = 113
n_layers = 1
n_heads = 3
d_model = 32
d_ffn = 128

lr = 0.001
weight_decay = 1.0
steps = 21  # Reduced for testing
eval_interval = 10
seed = 43

n_examples = 12769
frac_train = 0.3

wandb_project = "modular-test" # No W&B logging for testing, but section kept for consistency