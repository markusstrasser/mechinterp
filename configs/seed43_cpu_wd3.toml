# Rerun of e332cujg (98.4% model) but on CPU instead of MPS
# Goal: Test if device (CPU vs MPS) causes the 98.4% vs 100% difference

# --- Model ---
p = 113
n_layers = 1
n_heads = 3
d_model = 32
d_ffn = 128

# --- Training ---
lr = 0.001
weight_decay = 3.0
steps = 60001
eval_interval = 100
checkpoint_intervals = [100, 500, 1000, 2000, 3000, 4000, 5000, 7500, 10000, 15000, 20000, 25000, 30000, 35000, 40000, 45000, 50000, 55000, 60000]
seed = 43

# --- Dataset ---
n_examples = 12769
frac_train = 0.3

# --- W&B ---
wandb_project = "mod-arith-grokking"

# --- Compute ---
device = "cpu"
